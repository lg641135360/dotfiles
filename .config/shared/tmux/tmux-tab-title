#!/bin/sh
set -eu
set -f

MAX_LABEL_LEN=${TMUX_TAB_TITLE_MAX:-28}

strlen() {
    printf '%s\n' "$1" | awk '{ print length }'
}

truncate_tail() {
    value=$1
    max=$2

    [ "$(strlen "$value")" -le "$max" ] && {
        printf '%s\n' "$value"
        return 0
    }

    if [ "$max" -le 3 ]; then
        printf '%s\n' "$value" | awk -v max="$max" '{ print substr($0, 1, max) }'
        return 0
    fi

    printf '%s\n' "$value" | awk -v max="$max" '{ print "..." substr($0, length($0) - (max - 3) + 1) }'
}

clean_remote_host() {
    host=$1

    host=${host#ssh://}
    host=${host#file://}
    host=${host%%/*}
    host=${host#*@}
    host=${host%%:*}
    host=${host%%.*}

    case "$host" in
        ''|localhost|127|0)
            return 1
            ;;
    esac

    printf '%s\n' "$host"
}

normalize_uri_path_and_host() {
    path=$1
    detected_host=''

    case "$path" in
        file://*/*|ssh://*/*)
            rest=${path#*://}
            detected_host=${rest%%/*}
            path=/${rest#*/}
            ;;
    esac

    printf '%s\t%s\n' "$path" "$detected_host"
}

shorten_path() {
    path=$1
    max=$2

    [ -n "$path" ] || path='~'

    case "$path" in
        "$HOME") path='~' ;;
        "$HOME"/*) path="~/${path#"$HOME"/}" ;;
    esac

    [ "$(strlen "$path")" -le "$max" ] && {
        printf '%s\n' "$path"
        return 0
    }

    base=${path##*/}
    dir=${path%/*}
    parent=${dir##*/}

    if [ -n "$parent" ] && [ "$parent" != "$dir" ]; then
        compact=".../$parent/$base"
    else
        compact=$path
    fi

    truncate_tail "$compact" "$max"
}

extract_ssh_host_from_args() {
    args=$1
    set -- $args

    seen_ssh=0
    skip_next=0
    for token do
        if [ "$seen_ssh" -eq 0 ]; then
            command_name=${token##*/}
            [ "$command_name" = ssh ] && seen_ssh=1
            continue
        fi

        if [ "$skip_next" -eq 1 ]; then
            skip_next=0
            continue
        fi

        case "$token" in
            --)
                continue
                ;;
            -[bcDEeFIiJLlmOoQpRSWw])
                skip_next=1
                continue
                ;;
            -*)
                continue
                ;;
            *)
                clean_remote_host "$token" || true
                return 0
                ;;
        esac
    done
}

detect_ssh_host_from_tty() {
    tty=$1
    [ -n "$tty" ] || return 0

    tty=${tty#/dev/}
    ssh_args=$(
        ps -t "$tty" -o stat= -o comm= -o args= 2>/dev/null |
            awk '$1 ~ /\+/ && ($2 == "ssh" || $0 ~ /(^|[[:space:]\/])ssh[[:space:]]/) { sub(/^[[:space:]]*[^[:space:]]+[[:space:]]+[^[:space:]]+[[:space:]]+/, ""); print; exit }'
    )

    [ -n "$ssh_args" ] || return 0
    extract_ssh_host_from_args "$ssh_args"
}

remote_from_title() {
    title=$1

    case "$title" in
        *@*:*)
            clean_remote_host "${title%%:*}" || true
            ;;
        [A-Za-z0-9_.-]*:*)
            clean_remote_host "${title%%:*}" || true
            ;;
    esac
}

path_from_title() {
    title=$1

    case "$title" in
        *@*:*) printf '%s\n' "${title#*:}" ;;
        [A-Za-z0-9_.-]*:*) printf '%s\n' "${title#*:}" ;;
    esac
}

format_label() {
    path=$1
    remote_host=${2:-}
    title=${3:-}

    uri_info=$(normalize_uri_path_and_host "$path")
    old_ifs=$IFS
    IFS=$(printf '\t')
    set -- $uri_info
    path=${1:-$path}
    uri_host=${2:-}
    IFS=$old_ifs

    if [ -z "$remote_host" ] && [ -n "$uri_host" ]; then
        remote_host=$(clean_remote_host "$uri_host" || true)
    else
        remote_host=$(clean_remote_host "$remote_host" || true)
    fi

    if [ -z "$remote_host" ] && [ -n "$title" ]; then
        remote_host=$(remote_from_title "$title" || true)
        title_path=$(path_from_title "$title" || true)
        [ -n "$title_path" ] && path=$title_path
    fi

    if [ -n "$remote_host" ]; then
        host_len=$(strlen "$remote_host")
        path_max=$((MAX_LABEL_LEN - host_len - 1))
        [ "$path_max" -lt 8 ] && path_max=8
        short_path=$(shorten_path "$path" "$path_max")
        printf '%s:%s\n' "$remote_host" "$short_path" | awk -v max="$MAX_LABEL_LEN" '{ if (length($0) > max) print substr($0, 1, max); else print }'
        return 0
    fi

    shorten_path "$path" "$MAX_LABEL_LEN"
}

if [ "${1:-}" = --format ]; then
    shift
    format_label "${1:-}" "${2:-}" "${3:-}"
    exit 0
fi

pane_target=${1:-}
format='#{pane_current_path}	#{pane_title}	#{pane_tty}'

if [ -n "$pane_target" ]; then
    pane_info=$(tmux display-message -p -t "$pane_target" "$format" 2>/dev/null || true)
else
    pane_info=$(tmux display-message -p "$format" 2>/dev/null || true)
fi

if [ -z "$pane_info" ]; then
    printf 'tmux\n'
    exit 0
fi

old_ifs=$IFS
IFS=$(printf '\t')
set -- $pane_info
pane_path=${1:-}
pane_title=${2:-}
pane_tty=${3:-}
IFS=$old_ifs

remote_host=$(detect_ssh_host_from_tty "$pane_tty" || true)
format_label "$pane_path" "$remote_host" "$pane_title"
