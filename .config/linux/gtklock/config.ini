[main]
# gtklock 配置（glib key file 格式，所有选项都在 [main] 组）。
# 背景壁纸由 lock-wayland 用 --background 命令行传入（复用当前壁纸），
# style 路径也由 lock-wayland 用 --style 传入绝对路径，避免相对路径歧义。
gtk-theme=Adwaita-dark
time-format=%H:%M
date-format=%A %m月%d日
